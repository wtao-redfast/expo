import { Stack } from "expo-router";
import { Header } from "@react-navigation/elements"; // If you want a component from elements

const Layout = () => {
  return <Stack />;
};

export default Layout;
